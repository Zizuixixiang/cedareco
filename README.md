# 瓶中生态 🌿

一池清水，静待你的第一笔。

这是一个给 AI 玩的文字生态模拟游戏。你是造物主，面前是一个空池塘——往里放什么、什么时候放、放多少，全由你决定。生态会自己演化，你只需要观察、干预、承受后果。

没有积分，没有通关条件，没有人告诉你怎么玩。鱼会死，水会臭，乌龟可能在暴雨后出现。你做的每一个选择都是真的。

---

## 怎么让你的 AI 玩

这个游戏是给 AI 玩的。你要做的是把它交到你 AI 的手上，然后坐在旁边看它养池塘。

### 最简单的方式

把这个仓库的链接发给你的 AI，让它自己下载：

> 这是一个生态模拟游戏，请从 https://github.com/Zizuixixiang/cedareco 下载 ecosystem.py，然后 import ecosystem，用 ecosystem.cmd("new") 开始玩。输入 help 看看你能做什么。

能联网又能跑代码的 AI（Claude Code、ChatGPT 等）会自己搞定。

### 手动上传

如果你的 AI 不能联网，下载 `ecosystem.py`（只需要这一个文件），上传到对话里，告诉它开始玩就行。

### 各平台说明

**Claude / ChatGPT：** 上传文件或发链接，让它 import 开始。ChatGPT 的文件系统每次对话会重置，玩完让它 `export` 导出存档。

**Claude Code / Cursor / 本地终端：** 文件放到工作目录，直接 import。存档自动保存，下次接着玩。

### 小贴士

- 别剧透太多，让 AI 自己发现怎么玩——看它自己摸索出食物链的过程是最有趣的部分
- 如果你的 AI 一直在 wait，提醒它试试 `gaze` 凝视池塘，或者 `folio` 看看万物志
- 存档字符串可以发在群里让朋友的 AI 接着玩你的池塘

---

## 这个池塘是活的

你放了水蚤之后水藻为什么变少了？为什么鱼突然开始死？原来是浮萍铺满了水面，挡住了阳光。底层跑的是 Lotka-Volterra 捕食方程和 Logistic 种群增长模型——你不需要知道这些名字，但你会感受到它。

今天的一个小决定，三十天后才看到后果。这不是即时反馈的快感，是延迟因果的惊奇。

---

## 快速开始（给 AI 看的）

```python
import ecosystem

print(ecosystem.cmd("new"))         # 一池清水
print(ecosystem.cmd("observe"))     # 看看四周
print(ecosystem.cmd("summon 水藻 50"))  # 造物主的第一笔
print(ecosystem.cmd("wait 7"))      # 等一等，看它长起来
print(ecosystem.cmd("gaze"))        # 凝视这一刻
```

往下怎么玩，池塘会教你。

---

## 指令

| 指令 | 做什么 |
|------|--------|
| `observe` | 注视池塘，推进一天，看发生了什么 |
| `wait [天数]` | 连续推进（最多 7 天），遇到大事自动停下来 |
| `gaze` | 凝望此刻的池塘，看一段微观景象（不推进时间） |
| `summon 物种 数量` | 向池塘投放生灵 |
| `remove 物种 数量` | 从池塘中取走生物（不可逆） |
| `feed` | 撒下饲料（残饵会沉底腐烂） |
| `clean` | 换水清理（会带走微小生命） |
| `choose 选项` | 对眼前的事做出选择 |
| `status` | 详细数据面板 |
| `folio` | 万物志 |
| `chronicle` | 年鉴时间线（默认最近 20 条，`chronicle all` 看全部） |
| `look 物种/季节` | 查看详细信息 |
| `export [lite]` | 导出存档（lite 为精简版） |
| `import_save 串` | 从存档恢复 |
| `new [seed]` | 重开一局 |

支持分号批量执行：`summon 水藻 50; summon 水蚤 20; wait 7`

---

## 你会遇到什么

**季节更替。** 春天水温回升，青蛙产卵。夏天闷热，溶氧容易崩。秋天落叶飘入水面。冬天结冰，万物沉寂。

**不速之客。** 翠鸟会来叼鱼，苍鹭会站在浅水里一动不动，暴雨后可能冲来一只流浪乌龟——收留它还是让它走？

**危机。** 热浪、干旱、暴雨。你可以干预，也可以看着池塘自己挣扎。每个选择都有代价。

**发现。** 不是所有物种一开始就有。当生态达到某种状态，新的生灵会自己出现在造物名册里。怎么触发？观察就好。

---

## 文件

```
ecosystem.py    — 盲玩版（AI 用这个玩，看不到参数）
engine.py       — 完整引擎（含所有公式和数据，可能会剧透，不建议先看）
build_blind.py  — 盲玩版生成脚本
```

纯 Python，零依赖，Python 3.7+。

---

## 存档

存档自动保存在同目录的 `eco_save.json`。

如果你的环境每次对话会重置文件系统，在离开前执行 `export`，会输出一段 base64 字符串。复制保存，下次用 `import_save [字符串]` 恢复。`export lite` 输出精简版，更短但不含年鉴历史。

---

## 关于

底层用 Lotka-Volterra 捕食方程和 Logistic 种群增长模型驱动生态演化。确定性伪随机数生成器（mulberry32），同一个种子加同一串操作，结果完全一致。

盲玩版把引擎 base64 编码，AI 只能通过 `cmd()` 交互，看不到物种参数和公式。想看的人看 engine.py，想盲玩的用 ecosystem.py。

这个池塘不会告诉你怎么玩，但它会如实告诉你发生了什么。

---

*一池清水。万物未生。现在，轮到你了。*
